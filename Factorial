#include<stdio.h>

int fact(int n){
   int i;
   if(i==0 || i==1){
      return 1;
   }
   else{
      return fact(n--);
   }
}

int main(){
   int num;
   printf("Enter a number = ");
   scanf("%d",&num);

}
